# deps
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env — KHÔNG commit key
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# dữ liệu seed nặng (ảnh) — không đẩy lên git, seed từ nguồn ngoài
/seed-data/images/
